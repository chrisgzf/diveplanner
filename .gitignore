# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Bun
# (bun.lock is committed; ignore only debug logs)
bun-debug.log*

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env.*
!.env.example

# Vite / tooling caches
.vite/
*.local
coverage/
.eslintcache

# TypeScript build info (project references)
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# Installed agent skills — materialized from skills-lock.json (committed).
# Run `bunx --bun skills add <source>` to restore. The .claude/skills symlink
# points at an absolute machine path, so it must never be committed.
.agents/
.claude/skills/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~
*.un~
.DS_Store
Thumbs.db
